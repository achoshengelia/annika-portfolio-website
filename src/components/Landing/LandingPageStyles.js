import styled from 'styled-components';
import { CenterWrapperStyled } from '../../components/global/utils';

export const ContainerStyled = styled.main`
  height: 100%;

  ${CenterWrapperStyled} {
    height: 100%;
  }
`;
